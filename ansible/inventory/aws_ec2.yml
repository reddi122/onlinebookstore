plugin: amazon.aws.aws_ec2

cache: false

regions:
  - us-west-1

filters:
  instance-state-name: running

hostnames:
  - ip-address

compose:
  ansible_host: public_ip_address

keyed_groups:
  - key: tags.Role   # 🔥 capital R matches AWS tag
    prefix: ""
    separator: ""
